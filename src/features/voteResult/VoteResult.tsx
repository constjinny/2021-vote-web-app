import { ReactElement } from "react";
import styled from "@emotion/styled";

export function VoteResult(): ReactElement {
  return <VoteResultStyle>VoteResult</VoteResultStyle>;
}

const VoteResultStyle = styled.div``;
