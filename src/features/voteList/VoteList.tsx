import { ReactElement } from "react";
import styled from "@emotion/styled";

export function VoteList(): ReactElement {
  return <VoteListStyle>VoteList</VoteListStyle>;
}

const VoteListStyle = styled.div``;
