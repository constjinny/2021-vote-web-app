import { ReactElement } from "react";
import styled from "@emotion/styled";

export function VoteDetail(): ReactElement {
  return <VoteDetailStyle>VoteDetail</VoteDetailStyle>;
}

const VoteDetailStyle = styled.div``;
